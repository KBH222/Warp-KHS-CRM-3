# Test Environment Variables for Playwright

# API URL for testing
VITE_API_URL=http://localhost:3000

# Test auth token (if backend supports token-based auth in test mode)
TEST_AUTH_TOKEN=test-auth-token-12345

# Test user credentials
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=testpassword123

# Skip auth in test mode (if backend supports this)
SKIP_AUTH=true
TEST_MODE=true